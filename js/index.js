"use strict";

//API-URL
//https://data.fixer.io/api/latest?access_key=7a08874ca073ab28b507874d92019734

const calculator = {
    rates: [],
    from: null,
    to: null,
    init: async function() {
        //Voeg een API call toe, sla de rates op in this.rates

        //Maak 2 instanties aan van de Currency-klasse, deze bewaar je in de variabelen this.from en this.to

        //Voeg een eventListener toe voor de convertBtn
    },
    convert: function() {
        console.log('Convert');
    }
};

class Currency {
    constructor(name, rates) {

    }
}

calculator.init();